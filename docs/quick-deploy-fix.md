# Быстрое решение проблемы автодеплоя

## 🔍 Диагностика (1 минута)

Подключитесь к серверу и выполните:

```bash
# Скопируйте скрипт диагностики на сервер
scp check-deploy.sh user@testdandypizza.com:/var/www/testdandypizza/

# Запустите диагностику
ssh user@testdandypizza.com "bash /var/www/testdandypizza/check-deploy.sh"
```

Скрипт покажет, что именно не работает.

---

## ⚡ Быстрое решение #1: Cron (2 минуты)

Самый простой способ - добавить cron задачу на сервере:

```bash
# Подключитесь к серверу
ssh user@testdandypizza.com

# Откройте crontab
crontab -e

# Добавьте эту строку (проверка каждые 5 минут):
*/5 * * * * /bin/bash /var/www/testdandypizza/deploy.sh >> /var/www/testdandypizza/deploy.log 2>&1

# Сохраните (Ctrl+O, Enter, Ctrl+X в nano)

# Проверьте, что задача добавлена:
crontab -l
```

**Готово!** Теперь каждые 5 минут сервер будет проверять обновления на GitHub.

**Проверка:**
```bash
# Подождите 5 минут и проверьте логи:
tail -f /var/www/testdandypizza/deploy.log
```

---

## ⚡ Быстрое решение #2: GitHub Actions (5 минут)

Более быстрый способ - деплой сразу при push:

### Шаг 1: Создайте SSH ключ

На вашем компьютере:
```bash
ssh-keygen -t ed25519 -C "github-deploy" -f ~/.ssh/github_deploy_key
# Просто нажимайте Enter (без пароля)
```

### Шаг 2: Добавьте ключ на сервер

```bash
# Скопируйте публичный ключ
cat ~/.ssh/github_deploy_key.pub

# Подключитесь к серверу и добавьте ключ
ssh user@testdandypizza.com
echo "ваш_публичный_ключ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
exit
```

### Шаг 3: Добавьте секреты в GitHub

1. Откройте https://github.com/snowkukolkina-sudo/repo-02-01-2026/settings/secrets/actions
2. Нажмите **New repository secret**
3. Добавьте три секрета:

| Name | Value |
|------|-------|
| `DEPLOY_HOST` | IP адрес или домен сервера (например: `testdandypizza.com`) |
| `DEPLOY_USER` | Пользователь SSH (например: `root` или `ubuntu`) |
| `DEPLOY_SSH_KEY` | Содержимое файла `~/.ssh/github_deploy_key` (приватный ключ) |

Чтобы скопировать приватный ключ:
```bash
# Windows (PowerShell):
Get-Content ~/.ssh/github_deploy_key | Set-Clipboard

# Linux/Mac:
cat ~/.ssh/github_deploy_key | pbcopy
# или
cat ~/.ssh/github_deploy_key
```

### Шаг 4: Готово!

Workflow уже создан в `.github/workflows/deploy.yml`. После смержа в main деплой будет происходить автоматически.

**Проверка:**
1. Сделайте любой коммит и push в main
2. Откройте https://github.com/snowkukolkina-sudo/repo-02-01-2026/actions
3. Увидите запущенный workflow "Auto Deploy to Server"

---

## 🧪 Ручная проверка деплоя

Если хотите проверить, что скрипт работает:

```bash
ssh user@testdandypizza.com
cd /var/www/testdandypizza
bash deploy.sh
```

Должно вывести:
```
🔄 Обнаружены изменения, обновляем...
📦 Устанавливаем зависимости...
🔄 Перезапускаем приложение...
✅ Обновление завершено: Wed Jan  8 12:34:56 UTC 2026
```

Или если изменений нет:
```
✨ Изменений нет: Wed Jan  8 12:34:56 UTC 2026
```

---

## 🔧 Проверка текущего состояния

```bash
# Проверить, какой коммит на сервере
ssh user@testdandypizza.com "cd /var/www/testdandypizza && git log -1 --oneline"

# Проверить, какой коммит на GitHub
git log origin/main -1 --oneline

# Если они разные - деплой не работает!
```

---

## 📊 Мониторинг деплоя

### Cron логи:
```bash
tail -f /var/www/testdandypizza/deploy.log
```

### GitHub Actions:
https://github.com/snowkukolkina-sudo/repo-02-01-2026/actions

### PM2 статус приложения:
```bash
ssh user@testdandypizza.com "pm2 status"
```

---

## ❓ FAQ

**Q: Как часто будет проверяться GitHub при использовании cron?**
A: Каждые 5 минут. Можно изменить на `*/2` (каждые 2 минуты) или `* * * * *` (каждую минуту).

**Q: Какой способ лучше - cron или GitHub Actions?**
A: GitHub Actions - мгновенный деплой при push, логи в GitHub. Cron - проще настроить, но с задержкой до 5 минут.

**Q: Можно ли использовать оба способа?**
A: Да, но это избыточно. Выберите один.

**Q: Как отключить автодеплой?**
A: Cron: `crontab -e` и удалите строку. GitHub Actions: удалите файл `.github/workflows/deploy.yml`.

---

## 🚨 Если ничего не работает

1. Проверьте права на deploy.sh:
   ```bash
   chmod +x /var/www/testdandypizza/deploy.sh
   ```

2. Проверьте, что Git может pull:
   ```bash
   ssh user@testdandypizza.com "cd /var/www/testdandypizza && git pull origin main"
   ```

3. Проверьте, что PM2 работает:
   ```bash
   ssh user@testdandypizza.com "pm2 list"
   ```

4. Проверьте логи PM2:
   ```bash
   ssh user@testdandypizza.com "pm2 logs dandy-pizza --lines 50"
   ```

---

*Создано: 2026-01-08*
